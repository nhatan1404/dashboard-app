export enum TypeCoupon {
  FIXED = 'FIXED',
  PERCENT = 'PERCENT',
}

export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum Role {
  Admin = 'admin',
  Customer = 'customer',
  Employee = 'employee',
}
